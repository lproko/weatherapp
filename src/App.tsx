import { Flex } from "@mantine/core";
import "./App.css";
import MainPage from "./pages/MainPage";

const App = () => {
  return (
    <Flex>
      <MainPage />
    </Flex>
  );
};

export default App;
